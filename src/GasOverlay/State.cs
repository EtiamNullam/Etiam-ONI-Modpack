using Common;

namespace GasOverlay
{
    public static class State
    {
        public static Core Common = new Core("GasOverlay", "1737859934", null, false);

        public static Config Config = new Config();
    }
}
